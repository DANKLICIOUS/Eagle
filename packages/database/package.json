{
  "name": "@plate/database",
  "version": "0.1.0",
  "description": "PLATE database schema and types",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "supabase migration up"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.6",
    "vitest": "^1.0.4",
    "@vitest/ui": "^1.0.4"
  }
}
